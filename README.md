# UIT-ProjectManager-Team-10
## Backend
dependencies: 
Spring Web → REST API
Spring Data JPA → ORM / Database
Spring Security → Authentication
MySQL Driver -> Database
Lombok → giảm boilerplate code
Spring Boot DevTools → auto reload khi dev
