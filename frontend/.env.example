VITE_API_BASE_URL=http://localhost:8080/api

# Mock mode: dùng dữ liệu mock (src/mocks) thay cho backend thật.
# Bật mặc định khi không đặt; đặt 'false' để gọi backend ở VITE_API_BASE_URL.
VITE_USE_MOCK=true
